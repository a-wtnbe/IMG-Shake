<% @users.each do |user| %>
  <div>
    <%= link_to user_path(user) do %>
      <p>name: <%= user.name %></p>
      <%= attachment_image_tag user, :profile_image, :fill, 50, 50, format: 'jpeg' %>
    <% end %>
  </div>
<% end %>