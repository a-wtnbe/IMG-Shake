

<%= form_with(model: [article, comment], url: article_comments_path(@article) ) do |form| %>
  <div>
    <%= form.text_area :content, size: "7x5", class: "form-control"  %>
  </div>
    <%= form.submit "送信" %>
<% end %>
