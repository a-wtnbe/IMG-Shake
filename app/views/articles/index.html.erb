<% @articles.each do |article| %>
<div>
  <p>ユーザーネーム：<%= article.user.name %></p>
  <%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format: 'jpeg' %>

  <%= link_to article_path(article.id) do %>
    <%= attachment_image_tag article, :image, :fill, 100, 100, format: 'jpeg' %>
    <p>タイトル：<%= article.title %></p>
    <p>説明：<%= article.body %></p>
  <% end %>
</div>
<% end %>

<!--<div id="index_favorite_</%= @article.id %>">-->
<!--    </%= render 'favorites/favorite', article: article %>-->
<!--</div>-->

<%= render 'favorites/favorite', article: @article %>